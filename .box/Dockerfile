# Someone else's image, chosen because it is genuinely not box's: Ubuntu
# rather than Debian, its own JAVA_HOME, and no node, tmux, jq or git.
# box appends what it needs to drive an Agent in here.
FROM eclipse-temurin:21-jdk
