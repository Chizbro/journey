// Versions are a starting point, not a claim. If Gradle sync complains, let Android
// Studio's AGP Upgrade Assistant bump them — none of the probe's logic depends on these.
plugins {
    id("com.android.application") version "8.7.0" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
